version: '2.2'
services:
    app:
        build: .
        command: python room_ba.py
        volumes:
            - .:/ap
            - /etc/timezone:/etc/timezone:ro
            - /etc/localtime:/etc/localtime:ro
        environment:
            - TOKEN=
            - ICS_URL=
            - HEURE=